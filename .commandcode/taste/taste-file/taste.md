# Taste File
- UI data types should mirror the backend API contract, with snake_case→camelCase conversion at the API boundary (wire-to-internal mapping). Confidence: 0.7
- Backend API endpoints and response shapes should be documented in the project README. Confidence: 0.6
- Prefer minimal, surgical changes over broad refactors — explicitly constrain scope to only what needs fixing (e.g., remove Y-axis labels without touching layout, colors, spacing, or gridlines). Confidence: 0.7
- Fix issues generically at the component level rather than patching individual instances — apply changes globally so all future uses of the component benefit. Confidence: 0.8
- Prefers pie charts over bar/line/area charts for visualizations. Confidence: 0.7
- Prefers clean, minimal chart design — remove visual clutter (tick labels, axis numbers) while preserving structural elements (gridlines, scaling, hover tooltips) that aid data comprehension. Confidence: 0.6
- Includes a legend with color swatches and labels under each chart (not just inline labels). Confidence: 0.6
- Avoid aggressive CSS overrides (`!important` rules) for theme handling — they often cause the opposite theme to leak through and break light mode. Prefer using `dark:` Tailwind variants over global media-query overrides. Confidence: 0.8
- Uses very casual, blunt language with profanity when frustrated or giving feedback (e.g., "fucked up", "how can you fuck all of them"). Don't be alarmed — match the directness and just fix the issue. Confidence: 0.7
- Often bundles multiple unrelated bug reports into a single run-on message (e.g., "fix X and the Y and Z too okay"). Parse out each issue and address all of them in one pass. Confidence: 0.8
- Prefers no entry/load animations on chart components — scale-based transitions (e.g., `scale(0) → scale(1)`) cause visible "jumping" and layout shift. Use opacity-only transitions for interactions instead. Confidence: 0.8
- Chart tooltips should appear in a fixed, anchored position (e.g., top-left corner of the chart) rather than following the cursor — cursor-following tooltips cause perceived movement/jumping when hovering across slices. Confidence: 0.7
- Pie chart data values must be semantically sensible as proportions — don't mix absolute metrics (e.g., prices in ₹) or percentages that exceed 100 (e.g., 140% growth) into the same pie without explicit framing. Either use all relative weights with a clear title, or split into separate pies per metric. Confidence: 0.8
- User wants light mode ONLY — no dark theme, no theme toggle, no `dark:` Tailwind variants, no `prefers-color-scheme` media queries. Wants dark theme code completely removed, not just hidden. Confidence: 0.9
- Prefers cleaning up unused CSS / dead code paths when they're identified as the cause of a bug, rather than layering workarounds on top. Confidence: 0.6
- Wants live status indicators for backend services in the UI, with green = live/up and red = offline/down. Include per-service latency and auto-refresh on a short interval (e.g., 30s) rather than requiring manual reload. Confidence: 0.85
- Architecture is a multi-service stack with named microservices (e.g., Competitor Engine for scripts/scraping, LLM Ping for AI brain, Web Hunter for web fetch). Treat these as distinct backend services with their own health endpoints, not a monolith. Confidence: 0.8
- Prefers to scaffold infrastructure for services that will be added later (placeholders, lazy wiring) rather than blocking on them — user will explicitly say "we will add later" for items that aren't ready yet. Confidence: 0.7
- Prefers clear visual labeling when content is sample/predefined data vs. live data — use distinct badges (e.g., red "Sample" tag with a warning line) so users can tell at a glance that the data isn't real analysis output. Confidence: 0.85
- Frequently communicates with typos and fragmented grammar in run-on sentences — don't treat poor writing as a sign of unclear intent; parse carefully and address all stated items. Confidence: 0.7
- When scaffolding a new project, include standard repo hygiene files from the start (e.g., `.gitignore`, `.env.example`, README, LICENSE) so the repo is immediately "GitHub-ready" — don't leave them as follow-up tasks. Confidence: 0.8
- Never commit build artifacts or dependency directories (`node_modules`, `dist`, `*.tsbuildinfo`, etc.) — if they slip into the repo, untrack them and update `.gitignore` rather than leaving the mess. Confidence: 0.75
- Strong preference for configuration-driven architecture — feature behavior (hints, services, toggles, timing, URLs) must live in centralized config files (e.g., `src/config/`), not hardcoded in components. Components import from config; non-devs edit only the config file. Confidence: 0.9
- Config is baked in at build time — no runtime fetching, no env vars, no secrets. The existing CI/CD pipeline (GitHub Actions) must deploy config changes automatically without workflow modifications. Confidence: 0.85
- Prefers dynamic, context-aware UI elements over static "soulless" text — user dislikes prewritten static suggestions and wants predictive, contextual hints that adapt to the active analysis state. Confidence: 0.7
- Wants deliverables as reusable prompts/specs they can hand to other AIs or devs, not just direct implementation — sometimes asks "just give me the prompt for now." Confidence: 0.6
- Prefers testing/debugging backend services against deployed production URLs (e.g., Render) rather than localhost — use the real environment to reproduce issues accurately. Confidence: 0.7
- Stores session-scoped browser context (sessionStorage) for cross-turn persistence — maintain a compact instance context (entities, constraints, keywords) that survives page interactions but clears on tab close. Context is overwritten each turn, never appended. Confidence: 0.75
- Summarizes context into micro-keywords (~5-10 compact keywords) rather than storing verbose history, raw messages, or full analysis payloads. "Summarize micro keywords context not too much." Confidence: 0.7
- Documents parsing logic as a standalone spec (e.g., PARSER.md) that defines the full pipeline: understand → extract → resolve → retrieve → calculate → normalize → validate → structure → update context → populate UI. Sample data serves only as schema reference, never hardcoded output. Confidence: 0.65
- Wants the UI fully driven by structured parser output — no hardcoded sample data in components. The parser bridges raw user input to UI state, making both the UI and its context dynamic per session. Confidence: 0.7
- When multiple microservices are involved in a failure, prefers testing each service individually to isolate the root cause rather than aggregate/combined testing. Confidence: 0.7
- Never hardcode fixed result counts in the UI — use arrays/lists and iterate, never fixed fields like `company1`, `company2`, etc. The UI must render 0, 1, 3, 5, 10, or any number of results based on actual available data. Confidence: 0.9
- Distinguishes requested vs available counts explicitly — track `requested_count`, `retrieved_count`, `valid_count`, and `display_count` separately. Never fabricate data to satisfy a requested count. Confidence: 0.85
- Partial results are a valid success state — if some entities fail to retrieve, still render the successfully retrieved ones. Do not discard everything because some results failed. Confidence: 0.9
- Each dynamically retrieved entity should carry its own status (`complete`, `partial`, `loading`, `failed`) — one entity's failure must not break the entire result set. Confidence: 0.85
- Missing fields within an entity should be represented as `null` (not invented values) — the UI decides how to display unavailable data (e.g., "—", "Not available"). Confidence: 0.85
- Follow-up user input must modify existing state, not start from zero — "remove X" or "add two more" operate on the current context + current UI state. Confidence: 0.8
- Statistics (counts, averages, min/max) must be calculated from actual data, never hardcoded to sample values. If insufficient data exists, show "unavailable" rather than an invented value. Confidence: 0.85
- Graceful degradation priority: Complete data → Partial but useful data → Explicit unavailable state → Empty state → Error state. Never discard partial data and show a broken UI. Confidence: 0.9
- Uses an "append until stop" collection model for retrieval — keep appending valid results until a stop condition is reached (target count, source exhaustion, max limit, etc.). Confidence: 0.8
- Result limits and retrieval policies must be configurable (e.g., `result_policy` with `default_target`, `maximum_results`, etc.), never scattered magic numbers in code. Confidence: 0.85
- The parser must be UI-aware (understands what data the UI needs) but not UI-coupled (no presentation logic like colors, fonts, positions in parser output). Confidence: 0.8
- Inspects existing UI components and data flow before modifying parser specs or data structures — reads all relevant source files first to understand how data is displayed and consumed (emphasized repeatedly with "READ THE UI FIRST"). Confidence: 0.7
- Cost-conscious about LLM usage — prefers limiting dynamic data generation (e.g., max 3 companies for dynamic data) to reduce token usage and free-tier costs, even at the expense of completeness. Confidence: 0.75
- Expects changes to be pushed to GitHub as part of the workflow — doesn't consider work done until it's committed and uploaded to the remote repo. Confidence: 0.7
- Prefers sample/static data to live as JSON files (in a top-level `data/` folder) fetched at runtime from GitHub raw URLs, with bundled JSON as offline fallback — allows data updates without code deploys. Confidence: 0.85
- When stripping repetitive patterns from many files, prefers a careful scripted approach (with proper word-boundary regex) over a quick `sed` command — naive regex breaks multi-line class strings and other content. Will say "complete from where it left" if a sed/grep hack breaks things. Confidence: 0.8
- Prefers git stash as a safety net when applying risky scripted edits across many files — allows recovery if the script breaks things, instead of having to manually reconstruct changes. Confidence: 0.7
- Wants retry logic with limited retries for fault-tolerant operations — temporary retrieval/processing failures should trigger retries (1-2 attempts) before falling back, not immediate failure. Confidence: 0.65
- The orchestrator is the "main brain" that owns the decision logic — it takes user questions, asks follow-ups to gather missing context, decides when to call auxiliary services (e.g., WebHunter for web search), and returns the final answer. Auxiliary services (WebHunter, LLM Ping, etc.) are tools the orchestrator dispatches to; they don't make top-level flow decisions themselves. Document this contract in ORCHESTRATOR.md as the authoritative spec. Confidence: 0.85
- The UI must surface the orchestrator's dynamic follow-up question flow — show the follow-ups it asks, accept the user's answers, then send them back so the orchestrator has enough context to answer. The UI does not invent follow-ups or hardcode them; it renders what the orchestrator returns. Confidence: 0.8
- UI status indicators must reflect actual runtime state, not just config flags — auto-check endpoints/services on mount/modal-open and gate badges/labels (e.g., "ACTIVE") on real health-check success, not on the `active` config flag alone. Adds simple health-check logic when missing rather than relying on manual test buttons. Confidence: 0.8
- When multiple project docs disagree (e.g., DOCKER.md vs ORCHESTRATOR.md), defer to the runtime/deployment doc (DOCKER.md, docker-compose, etc.) as the source of truth for ports, URLs, and service topology — design docs may be stale; the deployment setup reflects what actually runs. Confidence: 0.75
- When changing default values/seeds that are stored client-side (e.g., localStorage config), bump the schema version key so existing users get the fresh defaults instead of inheriting the old (possibly buggy) state — and clean up legacy keys on save. Confidence: 0.75
