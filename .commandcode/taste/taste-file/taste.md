# Taste File
- UI data types should mirror the backend API contract, with snake_case→camelCase conversion at the API boundary (wire-to-internal mapping). Confidence: 0.7
- Backend API endpoints and response shapes should be documented in the project README. Confidence: 0.6
- Prefer minimal, surgical changes over broad refactors — explicitly constrain scope to only what needs fixing (e.g., remove Y-axis labels without touching layout, colors, spacing, or gridlines). Confidence: 0.7
- Fix issues generically at the component level rather than patching individual instances — apply changes globally so all future uses of the component benefit. Confidence: 0.8
- Prefers pie charts over bar/line/area charts for visualizations. Confidence: 0.7
- Prefers clean, minimal chart design — remove visual clutter (tick labels, axis numbers) while preserving structural elements (gridlines, scaling, hover tooltips) that aid data comprehension. Confidence: 0.6
- Includes a legend with color swatches and labels under each chart (not just inline labels). Confidence: 0.6
- Avoid aggressive CSS overrides (`!important` rules) for theme handling — they often cause the opposite theme to leak through and break light mode. Prefer using `dark:` Tailwind variants over global media-query overrides. Confidence: 0.8
- Uses very casual, blunt language with profanity when frustrated or giving feedback (e.g., "fucked up", "how can you fuck all of them"). Don't be alarmed — match the directness and just fix the issue. Confidence: 0.7
- Often bundles multiple unrelated bug reports into a single run-on message (e.g., "fix X and the Y and Z too okay"). Parse out each issue and address all of them in one pass. Confidence: 0.8
- Prefers no entry/load animations on chart components — scale-based transitions (e.g., `scale(0) → scale(1)`) cause visible "jumping" and layout shift. Use opacity-only transitions for interactions instead. Confidence: 0.8
- Chart tooltips should appear in a fixed, anchored position (e.g., top-left corner of the chart) rather than following the cursor — cursor-following tooltips cause perceived movement/jumping when hovering across slices. Confidence: 0.7
- Pie chart data values must be semantically sensible as proportions — don't mix absolute metrics (e.g., prices in ₹) or percentages that exceed 100 (e.g., 140% growth) into the same pie without explicit framing. Either use all relative weights with a clear title, or split into separate pies per metric. Confidence: 0.8
- Both dark AND light themes must work correctly — when fixing theme issues, verify both modes, not just dark mode. User expects symmetric theming support. Confidence: 0.7
- Prefers cleaning up unused CSS / dead code paths when they're identified as the cause of a bug, rather than layering workarounds on top. Confidence: 0.6
- Wants live status indicators for backend services in the UI, with green = live/up and red = offline/down. Include per-service latency and auto-refresh on a short interval (e.g., 30s) rather than requiring manual reload. Confidence: 0.85
- Architecture is a multi-service stack with named microservices (e.g., Competitor Engine for scripts/scraping, LLM Ping for AI brain, Web Hunter for web fetch). Treat these as distinct backend services with their own health endpoints, not a monolith. Confidence: 0.8
- Prefers to scaffold infrastructure for services that will be added later (placeholders, lazy wiring) rather than blocking on them — user will explicitly say "we will add later" for items that aren't ready yet. Confidence: 0.7
- Prefers clear visual labeling when content is sample/predefined data vs. live data — use distinct badges (e.g., red "Sample" tag with a warning line) so users can tell at a glance that the data isn't real analysis output. Confidence: 0.85
- Frequently communicates with typos and fragmented grammar in run-on sentences — don't treat poor writing as a sign of unclear intent; parse carefully and address all stated items. Confidence: 0.7
- When scaffolding a new project, include standard repo hygiene files from the start (e.g., `.gitignore`, `.env.example`, README, LICENSE) so the repo is immediately "GitHub-ready" — don't leave them as follow-up tasks. Confidence: 0.8
- Never commit build artifacts or dependency directories (`node_modules`, `dist`, `*.tsbuildinfo`, etc.) — if they slip into the repo, untrack them and update `.gitignore` rather than leaving the mess. Confidence: 0.75
- Strong preference for configuration-driven architecture — feature behavior (hints, services, toggles, timing, URLs) must live in centralized config files (e.g., `src/config/`), not hardcoded in components. Components import from config; non-devs edit only the config file. Confidence: 0.9
- Config is baked in at build time — no runtime fetching, no env vars, no secrets. The existing CI/CD pipeline (GitHub Actions) must deploy config changes automatically without workflow modifications. Confidence: 0.85
- Prefers dynamic, context-aware UI elements over static "soulless" text — user dislikes prewritten static suggestions and wants predictive, contextual hints that adapt to the active analysis state. Confidence: 0.7
- Wants deliverables as reusable prompts/specs they can hand to other AIs or devs, not just direct implementation — sometimes asks "just give me the prompt for now." Confidence: 0.6
- Prefers testing/debugging backend services against deployed production URLs (e.g., Render) rather than localhost — use the real environment to reproduce issues accurately. Confidence: 0.7
- When multiple microservices are involved in a failure, prefers testing each service individually to isolate the root cause rather than aggregate/combined testing. Confidence: 0.7
